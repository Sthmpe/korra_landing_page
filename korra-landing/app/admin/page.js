import AdminPortalClient from '../../components/landing/AdminPortalClient';

export const metadata = {
  robots: { index: false, follow: false },
};

export default function AdminPage() {
  return <AdminPortalClient />;
}
